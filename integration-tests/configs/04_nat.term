#{table => <<"test_nat">>,
  chains =>
      [#{name => <<"postrouting_nat">>,priority => 100,type => nat,
         hook => postrouting,policy => accept,
         rules => [masq]},
       #{name => <<"inbound">>,priority => 0,type => filter,hook => input,
         policy => drop,
         rules =>
             [ct_established_accept,
              {iifname_accept,<<"lo">>},
              {tcp_accept,22},
              icmp_accept]}]}.
