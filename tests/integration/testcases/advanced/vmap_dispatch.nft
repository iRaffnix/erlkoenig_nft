table inet test {
    map port_vmap {
        type inet_service : verdict
        elements = { 80 : accept, 443 : accept }
    }
    chain input {
        type filter hook input priority 0; policy drop;
        ct state established,related accept
        tcp dport vmap @port_vmap
    }
}
