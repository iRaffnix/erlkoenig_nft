table inet test {
    counter ssh {
        packets 0 bytes 0
    }

    counter http {
        packets 0 bytes 0
    }

    chain input {
        type filter hook input priority 0; policy drop;
        tcp dport 22 counter name "ssh" accept
        tcp dport 80 counter name "http" accept
    }
}
