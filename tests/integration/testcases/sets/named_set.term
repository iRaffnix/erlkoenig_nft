#{table => <<"test">>,
  sets => [{<<"blocklist">>, ipv4_addr, #{elements => [<<"198.51.100.1">>, <<"203.0.113.5">>]}}],
  chains => [
    #{name => <<"input">>, priority => 0, type => filter, hook => input,
      policy => drop,
      rules => [{set_lookup_drop, <<"blocklist">>}]}
  ]}.
